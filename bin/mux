#!/usr/bin/env bash

# ======================================================================
# Start a zellij session
# ======================================================================
if [ -n "$ZELLIJ_SESSION_NAME" ]
then
    echo Already inside session $ZELLIJ_SESSION_NAME
else
    zellij attach || zellij
fi


