[project]
name = "tasks"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fabric>=3.2.2",
    "invoke>=2.2.1",
]

[dependency-groups]
dev = [
    "ipython>=9.9.0",
    "ruff>=0.14.14",
    "ty>=0.0.14",
]
